## 样式调整
- 全局禁止拖拽选中变蓝：在 `*` 选择器添加 `user-select: none; -webkit-user-select: none;`

## 功能增强
- MakeupGen 图片预览：点击已勾选的图片框放大预览，点击空白区域关闭预览
  - 新增 `previewImage` 状态和 Teleport 模态框
  - `selectIdImage`/`selectRefImage`/`selectCachedPhoto` 均增加"已选中→预览"逻辑
