{
  "name": "aimirror",
  "private": true,
  "version": "0.0.1",
  "author": "Akise07 <1492354280@qq.com>",
  "homepage": "http://akise.cn",
  "main": "dist/electron/main/main.js",
  "scripts": {
    "vite:dev": "vite",
    "vite:build": "vue-tsc --noEmit && vite build",
    "vite:preview": "vite preview",
    "ts": "tsc",
    "watch": "tsc -w",
    "lint": "eslint -c .eslintrc --ext .ts ./src",
    "app:dev": "tsc && concurrently vite \" electron .\" \"tsc -w\"",
    "app:build": "npm run vite:build && tsc && electron-builder --linux --arm64",
    "app:preview": "npm run vite:build && tsc && electron ."
  },
  "build": {
    "appId": "YourAppID",
    "asar": true,
    "electronVersion": "40.6.1",
    "directories": {
      "buildResources": "assets",
      "output": "release/${version}"
    },
    "files": [
      "dist"
    ],
    "linux": {
      "artifactName": "${productName}-v${version}-${platform}-setup.${ext}",
      "target": [ "deb" ]
    },
    "mac": {
      "artifactName": "${productName}_${version}.${ext}",
      "target": [
        "dmg"
      ]
    },
    "win": {
      "target": [
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ],
      "artifactName": "${productName}_${version}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "perMachine": false,
      "allowToChangeInstallationDirectory": true,
      "deleteAppDataOnUninstall": false
    }
  },
  "dependencies": {
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.3.3",
    "concurrently": "^7.2.2",
    "electron": "^25.3.2",
    "electron-builder": "^24.6.3",
    "pinia": "^2.0.18",
    "typescript": "^4.5.4",
    "vite": "^2.9.9",
    "vue-router": "^4.1.3",
    "vue-tsc": "^0.34.7"
  },
  "repository": "https://github.com/Yukun-Guo/vite-vue3-electron-ts-template.git",
  "license": "MIT"
}
